{
  "name": "mobile-codespace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first Safari control surface for GitHub Codespaces",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
