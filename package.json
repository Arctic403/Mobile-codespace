{
  "name": "mobile-codespace-profile",
  "version": "2.0.0",
  "private": true,
  "description": "Mobile-first GitHub Codespaces / VS Code profile",
  "scripts": {
    "check": "node --check extension/extension.js && node scripts/validate-json.mjs",
    "package:extension": "mkdir -p dist && cd extension && npx --yes @vscode/vsce@latest package --out ../dist/mobile-codespace-profile.vsix"
  }
}
