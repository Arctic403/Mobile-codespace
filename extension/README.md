# Mobile Codespace Profile extension

Small local VS Code extension used by the Mobile Codespace Profile.

It contributes a single `Mobile` status-bar button and native layout commands. It does not implement an editor, terminal, Git client, authentication layer, or Codespaces transport.
